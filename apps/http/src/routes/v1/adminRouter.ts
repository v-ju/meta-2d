import { Router } from "express";

export const adminRouter:Router = Router();

adminRouter.get("/element")

adminRouter.put("/element/:elementId")

adminRouter.post("/avatar")

adminRouter.post("/map")